.navbar.navbar-default
  .container
    = link_to image_tag("perche-28-logo.png", alt: "Perche 2.8"), root_path, class: 'navbar-brand'
    %p.navbar-text naturellement innovants
    %ul.nav.navbar-nav.navbar-right
      - if contributeur_signed_in?
        %li.navbar-text= "Bonjour #{current_contributeur.pseudo}"
        - if current_contributeur.type == 'Animateur'
          %li= link_to "Animation", animation_path
        %li= link_to "Déconnexion", destroy_contributeur_session_path, method: :delete, class: 'btn btn-default'
      - else
        %li= link_to "Connexion", new_contributeur_session_path, class: 'btn btn-default'
  .ligne-rouge
